from django.contrib import admin

# Register your models here.
# from models import User

'''
class UserAdmin(admin.ModelAdmin):
    list_display = ('name', 'password', 'role', 'description', 'email', )

    fields = [
        ('Account', {'fields': ['name', 'password']}),
        ('Info', {'classes': ['collapse',], 'fields': ['role', 'description', 'email']}),
    ]

    list_filter = ['name', 'role', ]

    search_filter = ['name', 'role', ]
'''
